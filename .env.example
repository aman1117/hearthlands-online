# Copy to .env locally. Never commit real credentials.
# Leave DATABASE_URL empty to use the local SQLite database.
DATABASE_URL=
DATABASE_SCHEMA=hearthlands_game

# Use verified TLS for Azure PostgreSQL. "disable" is only for a local DB.
DATABASE_SSL=require

PORT=3000
HOST=0.0.0.0
ROOM_RETENTION_DAYS=90
